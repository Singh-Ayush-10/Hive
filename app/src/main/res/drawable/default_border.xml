<!-- default_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Define the border color and width for default state -->
    <stroke
        android:width="1dp"
        android:color="@color/default_border_color"/>
    <!-- You can also define other properties like corner radius, padding, etc. -->
</shape>
